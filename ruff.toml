# Default rule set (E4, E7, E9, F) with one exemption.
# E741 flags the single-letter name `l`, which is the model's labour-supply
# variable (l_policy, l_sim) and follows the paper's notation. Renaming it is a
# notation change, not a lint fix.
[lint]
ignore = ["E741"]
